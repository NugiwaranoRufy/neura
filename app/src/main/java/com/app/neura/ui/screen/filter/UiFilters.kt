package com.app.neura.ui.screen.filter

enum class ChallengeTypeFilter {
    ALL,
    LOGIC,
    LATERAL
}

enum class DifficultyFilter {
    ALL,
    EASY,
    MEDIUM,
    HARD
}

enum class PackSortOption {
    NEWEST,
    OLDEST,
    TITLE
}